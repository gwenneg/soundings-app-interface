{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "go -C ${CLAUDE_PLUGIN_ROOT} run . hook ${CLAUDE_PLUGIN_ROOT}"
          }
        ]
      }
    ]
  }
}
